services:
  danger:
    image: alpine:3.20
    privileged: true                              # block-severity guardrail
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock  # block-severity guardrail
    restart: unless-stopped
